<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
        Title="TPW Etap 1" Height="650" Width="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TimerText"
                   Text="Czas działania: 0.00 s"
                   FontSize="16"
                   FontWeight="Bold"
                   Margin="0 0 0 10"/>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" Grid.Row="1">
            <TextBlock Text="Ilość kul:" VerticalAlignment="Center" Margin="0 0 10 0"/>
            <TextBox x:Name="BallCountBox"
                     Width="60"
                     Height="30"
                     Text="10"
                     VerticalContentAlignment="Center"
                     Margin="0 0 20 0"/>

            <Button x:Name="StartButton"
                    Content="Start"
                    Width="100"
                    Height="30"
                    Click="Start_Click"/>
        </StackPanel>

        <Canvas x:Name="BallCanvas"
                Grid.Row="2"
                Background="White"
                SizeChanged="Canvas_SizeChanged">

            <Rectangle
                Stroke="Black"
                StrokeThickness="3"
                Fill="LightGray"
                Width="{Binding ActualWidth, ElementName=BallCanvas}"
                Height="{Binding ActualHeight, ElementName=BallCanvas}"/>

            <ItemsControl ItemsSource="{Binding Balls}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Left" Value="{Binding X}"/>
                        <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Ellipse Width="{Binding Radius}"
                                 Height="{Binding Radius}"
                                 Fill="{Binding Color}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </Canvas>

    </Grid>
</Window>
